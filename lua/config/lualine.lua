local lualine = require('lualine')

lualine.setup {
    options = {
        iconse_enabled = true,
        theme = 'onedark',
        component_separators = '|',
        section_separators = ' > ',
    },
}
