local lualine = require('lualine')

lualine.setup {
    options = {
        theme = 'github_dimmed',
        iconse_enabled = true,
        component_separators = '|',
        section_separators = ' > ',
    },
}
