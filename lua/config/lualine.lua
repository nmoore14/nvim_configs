local lualine = require('lualine')

lualine.setup {
    options = {
        iconse_enabled = true,
        theme = 'onenord',
        component_separators = '|',
        section_separators = ' > ',
    },
}
